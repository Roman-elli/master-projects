1. analise da estrutura do dataset, as classes estão equilibradas? pode ser preciso gerar novos dados


Explore solutions and compare the results using at least, the following
ANNs models:
– Multi-Layer Perceptrons (MLP)
– Convolutional Neural Networks (CNNs)
– Residual Neural Networks (ResNets)
•And for each model, experimentation with different layer architectures,
loss functions, optimisers and hyperparameters. Explore at least:
– Loss function: MultiMarginLoss, Cross Entropy
– Optimizers: RMSprop, ADAM